name: Go

on: [ push ]

jobs:

  build:
    runs-on: bufbuild/buf
    steps:
    - uses: actions/checkout@v2

    - name: Set up Go
      uses: actions/setup-go@v2
      with:
        go-version: 1.17

    - name: Build
      run: make build
